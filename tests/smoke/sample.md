# GlyphCull CI Smoke (Markdown)

A small Markdown document exercising the compiler's supported surface for the
CI smoke: headings, paragraphs, *emphasis*, **strong emphasis**, `inline code`,
[links](https://glyphcull.example), lists, a quote, a code block, and a
horizontal rule.

## Features

- Unordered item one
- Unordered item two
  - Nested item
- Ordered item one
- Ordered item two

> A block quote: the compiler preserves quoted text as a `quote` chunk.

```text
A literal code block: content is preserved verbatim.
```

---

Final paragraph after the rule.
